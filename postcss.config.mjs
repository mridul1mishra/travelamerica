// PostCSS config — Tailwind removed (was installed but never imported in any CSS file).
// Next.js provides autoprefixer automatically; no additional plugins needed.
const config = {
  plugins: {},
};

export default config;
